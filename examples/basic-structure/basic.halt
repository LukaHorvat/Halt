import Data

fn :: String -> Bool
fn str ->
    var len = List.length 100 str
    print ("Trace: " + len)
    if len > 5 then
        return True
    else    
        return False

main :: True
main unit ->
    for i from 1 to 10
        IO.print i
